# DISABLED ON PURPOSE: the whole workflow is commented out so that no
# plan or apply can run while the code is still being built.
# To re-enable, remove the leading '# ' from every line below this header.
#
# name: Terraform Plan

# # In one line: push to a feature branch -> read the stack folder from the
# #              commit message -> run "terraform plan" in that folder.
# #
# # Commit message convention:
# #   [<action>][<stack>] <short description> - Path: /<stack-folder>
# #   e.g. "[update][network] added a private subnet - Path: /resources/us-east-1/dev/network"
# #
# # The folder comes from the "Path: /..." part. The second [tag] is a
# # cross-check: a warning is printed when it does not match the folder name.
# #
# # Full walkthrough of every block: .github/workflows/README.md

# on:
#   push:
#     branches-ignore:
#       - main

# permissions:
#   contents: read    # checkout the repo
#   id-token: write   # let AWS verify this job via OIDC (no stored AWS keys)

# env:
#   AWS_REGION: us-east-1   # must match regional-values.yaml

# jobs:
#   plan:
#     runs-on: ubuntu-latest
#     steps:
#       - name: Checkout
#         uses: actions/checkout@v5

#       - name: Read stack folder from commit message
#         id: target
#         env:
#           COMMIT_MSG: ${{ github.event.head_commit.message }}
#         run: |
#           # "Path: /resources/us-east-1/dev/network" -> resources/us-east-1/dev/network
#           path="$(printf '%s' "$COMMIT_MSG" | grep -oiE 'path: *[^[:space:]]+' | head -1 | sed -E 's/^[Pp]ath: *//; s|^/||; s|/$||')"
#           # "[update][network]" -> network   (typo cross-check only: a bare
#           # tag cannot identify the environment, so it never picks the folder)
#           tag="$(printf '%s' "$COMMIT_MSG" | sed -nE 's/^\[[^]]+\]\[([^]]+)\].*/\1/p')"

#           if [ -z "$path" ] || [ ! -d "$path" ]; then
#             echo "::error::No valid stack folder in the commit message (Path: '/$path'). Expected e.g. '[update][network] ... - Path: /resources/us-east-1/dev/network'"
#             exit 1
#           fi
#           dir="$path"

#           # Warn if the [tag] does not match the stack folder's name
#           case "$(basename "$dir")" in
#             "$tag"*) ;;
#             *) echo "::warning::Bracket tag '$tag' does not match stack folder '$(basename "$dir")' - possible typo in the commit message" ;;
#           esac

#           # Child modules are not runnable roots - point the Path at the
#           # stack that consumes the module instead.
#           case "$dir" in
#             modules/*)
#               echo "::error::'$dir' is a child module, not a stack - name the stack that uses it (e.g. Path: /resources/us-east-1/dev/network)"
#               exit 1
#               ;;
#           esac

#           echo "Terraform will run in: $dir"
#           echo "dir=$dir" >> "$GITHUB_OUTPUT"

#       - name: Configure AWS credentials (OIDC)
#         uses: aws-actions/configure-aws-credentials@v5
#         with:
#           role-to-assume: ${{ vars.AWS_ROLE_ARN_PLAN }}   # read-only plan role - set this Actions variable to an existing or new role ARN; not managed in this repo
#           aws-region: ${{ env.AWS_REGION }}

#       - name: Setup Terraform
#         uses: hashicorp/setup-terraform@v3
#         with:
#           terraform_version: "1.12.2"   # PLACEHOLDER: pin to your version (>= 1.10 for S3 native state locking)

#       - name: Terraform Init
#         working-directory: ${{ steps.target.outputs.dir }}
#         run: terraform init -input=false
#         # PLACEHOLDER: enable once the state bucket exists.
#         # The bucket name is NOT committed (public repo - it contains the
#         # account id); put it in an Actions variable like the role ARNs.
#         # State keys mirror the repo layout, prefixed with the repo name:
#         #   <bucket>/<repo-name>/resources/us-east-1/dev/network/terraform.tfstate
#         #   terraform init -input=false \
#         #     -backend-config="bucket=${{ vars.TFSTATE_BUCKET }}" \
#         #     -backend-config="key=${{ github.event.repository.name }}/${{ steps.target.outputs.dir }}/terraform.tfstate" \
#         #     -backend-config="region=${{ env.AWS_REGION }}" \
#         #     -backend-config="use_lockfile=true"

#       - name: Terraform Validate
#         working-directory: ${{ steps.target.outputs.dir }}
#         run: terraform validate

#       - name: Terraform Plan
#         working-directory: ${{ steps.target.outputs.dir }}
#         run: terraform plan -input=false
